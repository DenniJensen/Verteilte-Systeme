\section{Test}
\label{sec:test}
Als Test für eine erfolgreiche Implementierung dienen die Ausgaben der
folgenden Machrichten auf der Konsole:
\begin{itemize}
  \item join
  \item accepted
  \item marker message arrived
  \item start snapshot / finish snapshot
  \item globale snapshot
\end

Der init Knoten ist als Leader bestimmt um den Snapshot zu starten. Für einen
Test erhalten alle Knoten einen Betrag von 100 wie das Beispiel in Listing
\ref{lst:testss} zeigt. Tests mit 3, 5, 20, 100 und 500 Knoten wurden
durchgeführt. Ein Test mit mehr als 500 Knoten konnte nicht beobachtet werden,
da zu viele Nachrichten ausgegeben wurden.
Zum testen wurden nur Nachrichten ausgetauscht, in denen keine Beträge
ausgetauscht wurden.
